import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

// Placeholder meal image component - you can replace this with an actual image file
const PlaceholderMeal = () => (
  <View style={styles.container}>
    <Text style={styles.emoji}>🍽️</Text>
    <Text style={styles.text}>Meal Image</Text>
  </View>
);

const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: '100%',
    backgroundColor: '#f0f0f0',
    justifyContent: 'center',
    alignItems: 'center',
  },
  emoji: {
    fontSize: 48,
    marginBottom: 8,
  },
  text: {
    fontSize: 16,
    color: '#666',
    fontWeight: '500',
  },
});

export default PlaceholderMeal;